# CR法收敛判据改进说明

## 修改内容

### 1. 新增收敛参数（SolverStatic.h）

在 `SolverStatic::Params` 中新增了以下参数：

```cpp
struct Params
{
    int numIncrements = 10;   // 荷载增量步数
    int maxIter = 32;         // 每个增量步的最大迭代次数
    double tol = 1e-5;        // 旧参数（向后兼容）
    double tol_R = 1e-4;      // 新增：相对残差容差
    double tol_dx = 1e-6;     // 新增：位移增量容差
    bool use_relative = true; // 新增：是否使用相对残差判据
    bool verbose = false;     // 新增：是否输出详细迭代信息
};
```

### 2. 改进的收敛判据（SolverStatic.cpp）

**旧版本**（只用绝对残差）：
```cpp
if (error < m_param.tol && iter > 0)
{
    // 收敛
}
```

**新版本**（相对残差 + 位移增量）：
```cpp
// 第一次迭代记录初始残差
if (iter == 0)
{
    norm_R0 = m_R.norm();
    if (norm_R0 < 1e-12) break;  // 初始残差已经很小
}

// 计算相对残差
double relative_R = (use_relative && norm_R0 > 1e-12) 
                    ? (norm_R / norm_R0) 
                    : norm_R;

// 双重判据
bool force_converged = (relative_R < tol_R);
bool disp_converged = (norm_dx < tol_dx);

if (force_converged && disp_converged && iter > 0)
{
    // 收敛
}
```

---

## 为什么这样改？

### 问题 1：绝对残差不适应不同规模的问题

**旧判据**：`|R| < 1e-5`
- 对于小问题（如单根梁）：可能太松，精度不够
- 对于大问题（如大型桁架）：可能太严，永远不收敛

**新判据**：`|R|/|R0| < 1e-4`
- 自适应问题规模
- 相对误差更合理

### 问题 2：只检查力平衡，不检查位移

在几何非线性问题中，可能出现：
- 残差很小（力平衡了）
- 但位移还在变化（还没真正收敛）

**新判据**增加了位移增量检查：`|dx| < 1e-6`

### 问题 3：初始残差处理不当

**旧判据**：`iter > 0` 强制至少迭代一次
- 如果初始残差已经很小，浪费计算

**新判据**：第一次迭代检查初始残差
- 如果 `|R0| < 1e-12`，直接收敛

---

## 参数调整建议

### 默认参数（已在代码中设置）

```cpp
p.tol_R = 1e-4;      // 相对残差：0.01% 的力平衡误差
p.tol_dx = 1e-6;     // 位移增量：微米级别
p.use_relative = true;
p.verbose = false;
```

### 如果迭代太慢（放松收敛条件）

```cpp
p.tol_R = 1e-3;      // 放松到 0.1%
p.tol_dx = 1e-5;     // 放松到 10 微米
```

### 如果需要高精度（收紧收敛条件）

```cpp
p.tol_R = 1e-5;      // 收紧到 0.001%
p.tol_dx = 1e-7;     // 收紧到 0.1 微米
```

### 调试模式（查看详细迭代信息）

```cpp
p.verbose = true;    // 输出每次迭代的残差和位移增量
```

---

## 输出信息解读

### 正常收敛

```
  增量步 1: 迭代 5 次收敛 (|R|/|R0|=8.23e-05, |dx|=3.45e-07)
```

**解读**：
- 迭代 5 次收敛
- 相对残差：8.23e-05 < 1e-4 ✓
- 位移增量：3.45e-07 < 1e-6 ✓

### 未收敛

```
警告: 增量步 1 未收敛 (|R|/|R0|=2.34e-03, |dx|=5.67e-05)
```

**可能原因**：
1. 增量步太大 → 减小 `m_StepSize`，增加 `numIncrements`
2. 刚度矩阵有问题 → 检查单元刚度计算
3. 荷载太大 → 分更多步加载

### 详细迭代信息（verbose = true）

```
    Iter 0: |R|/|R0|=1.00e+00, |dx|=1.00e+10
    Iter 1: |R|/|R0|=3.45e-01, |dx|=2.34e-03
    Iter 2: |R|/|R0|=1.23e-01, |dx|=8.76e-04
    Iter 3: |R|/|R0|=4.56e-02, |dx|=3.21e-04
    Iter 4: |R|/|R0|=1.67e-02, |dx|=1.23e-04
    Iter 5: |R|/|R0|=8.23e-05, |dx|=3.45e-07
```

**解读**：
- 残差单调下降 → 收敛正常 ✓
- 如果震荡或发散 → 刚度矩阵或步长有问题

---

## 使用示例

### 方式 1：使用默认参数（推荐）

```cpp
// 在 main.cpp 或输入文件中
pStructure->Add_AnalysisStep("Static", 1.0, 0.1, 1e-5, 32);
```

默认会使用：
- `tol_R = 1e-4`
- `tol_dx = 1e-6`
- `use_relative = true`

### 方式 2：手动调整参数（高级用户）

如果需要自定义参数，可以修改 `AnalysisStep.cpp` 中的设置：

```cpp
case EnumKeyword::StepType::STATIC:
{
    SolverNS::SolverStatic::Params p;
    p.numIncrements = static_cast<int>(m_Time / m_StepSize);
    if (p.numIncrements < 1) p.numIncrements = 1;
    p.maxIter = m_MaxIterations;
    
    // 自定义收敛参数
    p.tol_R = 1e-3;      // 根据问题调整
    p.tol_dx = 1e-5;     // 根据问题调整
    p.use_relative = true;
    p.verbose = true;    // 调试时打开
    
    solver = std::make_unique<SolverNS::SolverStatic>(p);
    break;
}
```

---

## 常见问题

### Q1：为什么我的 CR 法还是迭代很慢？

**A1**：收敛判据只是一方面，还需要检查：
1. 几何刚度矩阵的符号是否正确（特别是 Km2）
2. 应力刚度矩阵的计算是否有误
3. 增量步是否太大
4. 初始状态是否合理

### Q2：相对残差和绝对残差哪个更好？

**A2**：对于几何非线性问题，**相对残差更好**，因为：
- 自适应问题规模
- 不受单位制影响
- 工程软件（ABAQUS, ANSYS）的标准做法

### Q3：位移增量容差如何选择？

**A3**：取决于问题的特征长度：
- 小变形问题（mm 级）：`1e-6` ~ `1e-7`
- 大变形问题（m 级）：`1e-5` ~ `1e-6`
- 经验法则：约为特征长度的 1e-6 倍

### Q4：如何判断是收敛慢还是不收敛？

**A4**：看残差下降趋势：
- **收敛慢**：残差单调下降，但速度慢 → 调整参数或增量步
- **不收敛**：残差震荡或发散 → 刚度矩阵或荷载有问题

---

## 下一步优化方向

如果改进收敛判据后还是慢，可以考虑：

1. **检查几何刚度矩阵**：特别是 Km2 的符号
2. **增加线搜索（Line Search）**：防止 Newton 方向过大
3. **自适应增量步长**：根据迭代次数动态调整
4. **弧长法（Arc-Length）**：处理极限点和屈曲

---

## 修改文件清单

1. `YQY/Solver/Static/SolverStatic.h` - 新增参数定义
2. `YQY/Solver/Static/SolverStatic.cpp` - 改进收敛判据逻辑
3. `YQY/DataStructure/AnalysisStep/AnalysisStep.cpp` - 设置默认参数

---

**作者**：Claude  
**日期**：2026-04-15  
**版本**：v1.0
