# YQY 代码规范

本文件适用于仓库内全部自有代码。`ThirdPartyLibrary`、NuGet 包、Qt 自动生成文件和编译生成文件不在整改范围内。

## C++ 排版

- 使用仓库根目录的 `.clang-format`，最大行宽为 120。
- 函数返回类型、函数名和第一个参数应尽量位于同一行；仅在超过行宽时换行并对齐后续参数。
- 函数、类、结构体、命名空间和控制语句的左花括号必须另起一行。
- 不为追求短行而把只有少量参数的函数名和参数强制拆行。
- 头文件声明和源文件定义必须保持一致。

## 参数方向

- 继续使用项目既有的 `_OUT` 空宏标记输出参数。
- 函数通过非常量引用或指针向调用方返回结果时，在声明和定义中都必须添加 `_OUT`。
- 纯输入参数、`const` 引用和仅供函数内部使用的可变对象不得标记 `_OUT`。
- 输入输出混合参数必须根据实际语义判断，不允许仅依据类型机械添加。

## 函数与逻辑

- 保持既有数值算法、计算顺序、求解流程、文件格式和界面行为不变。
- 优先使用提前返回、提前继续和小型辅助函数减少嵌套；避免超过三层的非必要控制流嵌套。
- 同一离散值的多个互斥分支在更清晰时使用 `switch`；范围判断、浮点判断和复合条件不得强行改成 `switch`。
- 不在源文件开头通过匿名 `namespace` 或临时 `struct` 堆放新增辅助函数。
- 仅在单个源文件使用的辅助逻辑写成命名明确的文件内 `static` 函数；属于某个类的逻辑优先写成该类的私有成员函数。
- 被多个模块复用且不属于具体类的通用逻辑放入 `YQY/Utility`，并提供正式的头文件声明和源文件定义。
- 不为了减少行数引入复杂模板、宏、嵌套 Lambda 或难以理解的表达式。

## 注释与命名

- 自有代码中的说明性注释使用简洁中文，不写无必要的英文段落。
- 删除复述代码、临时调试、显而易见和已经失效的注释。
- 公式变量、标准名称、论文名称、类名、函数名、API、MPC、DOF、HDF5 等必要标识可以保留原文。
- 不擅自修改外部格式关键字、日志协议、序列化字段和用户可见文本。

## 验证

- 修改前识别并保留用户已有的未提交改动。
- 每批逻辑修改后至少执行一次对应配置的编译；涉及求解器、矩阵装配、导入导出时运行已有验证入口或相关测试。
- 不格式化或修改第三方代码、Qt 的 `moc/uic/rcc` 输出以及编译目录。
